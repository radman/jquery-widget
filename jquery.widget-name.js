/***
 * Widget Name
 *  Dependencies
 *   - (e.g. jQuery 1.6.1)
 *
 *  A description of the widget.
 *
 ***/

(function($){

  $.fn.widgetName = function(options) {
    $.each(this, function() {

      /* Private variables */

      var self = $(this);

      initialize();

      /* Private functions */

      function initialize() {

      }
    });

    /* Allow chaining */

    return this;
  };
  
})(jQuery);
